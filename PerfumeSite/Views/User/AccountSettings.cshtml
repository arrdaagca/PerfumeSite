<!DOCTYPE html>
<html lang="tr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Hesap Ayarlarım</title>
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
            font-family: 'Arial', sans-serif;
        }

        body {
            background: linear-gradient(135deg, #f8f9fa, #e9ecef);
            display: flex;
            justify-content: center;
            align-items: center;
            height: 100vh;
        }

        .container {
            background: white;
            width: 420px;
            padding: 25px;
            border-radius: 12px;
            box-shadow: 0 4px 10px rgba(0, 0, 0, 0.1);
            text-align: center;
        }

        h2 {
            margin-bottom: 15px;
            color: #343a40;
        }

        .tab-buttons {
            display: flex;
            flex-direction: column;
            gap: 10px;
            margin-bottom: 15px;
        }

            .tab-buttons a {
                display: block;
                text-decoration: none;
                text-align: center;
                padding: 12px;
                font-size: 14px;
                font-weight: bold;
                color: white;
                border-radius: 6px;
                transition: background 0.3s;
            }

            .tab-buttons .active-tab {
                background: #0056b3 !important;
            }

            .tab-buttons a:hover {
                opacity: 0.8;
            }

        .btn-blue {
            background: #007bff;
        }

        .btn-red {
            background: #dc3545;
        }

        .tab {
            display: none;
            padding: 15px;
            margin-top: 10px;
            background: #f8f9fa;
            border-radius: 8px;
        }

            .tab.active {
                display: block;
            }

        .input-group {
            margin-top: 10px;
            text-align: left;
        }

            .input-group label {
                display: block;
                font-weight: bold;
                margin-bottom: 5px;
                color: #495057;
            }

            .input-group input {
                width: 100%;
                padding: 10px;
                border: 1px solid #ced4da;
                border-radius: 6px;
                font-size: 14px;
            }

        .update-btn {
            width: 100%;
            padding: 10px;
            border: none;
            cursor: pointer;
            background: #28a745;
            color: white;
            border-radius: 6px;
            margin-top: 10px;
            font-size: 14px;
            transition: background 0.3s;
        }

            .update-btn:hover {
                background: #218838;
            }
    </style>
</head>
<body>

    <div class="container">
        <h2>Hesap Ayarlarım</h2>
        <div class="tab-buttons">
            <a asp-controller="Address" asp-action="Address" class="btn-blue active-tab" onclick="showTab('address')">Adres Bilgilerim</a>
            <a asp-controller="CreditCard" asp-action="CreditCard" class="btn-blue" onclick="showTab('cards')">Kayıtlı Kartlarım</a>
            <a href="#" class="btn-blue" onclick="showTab('emailChange')">E-Posta Değişikliği</a>
            <a href="#" class="btn-blue" onclick="showTab('passwordChange')">Şifre Değişikliği</a>
            <a href="/User/Login" class="btn-red">Çıkış Yap</a>
        </div>    
    </div>

    <script>
        function showTab(tabId) {
            document.querySelectorAll('.tab').forEach(tab => tab.classList.remove('active'));
            document.getElementById(tabId).classList.add('active');

            document.querySelectorAll('.tab-buttons a').forEach(btn => btn.classList.remove('active-tab'));
            event.target.classList.add('active-tab');
        }
    </script>

</body>
</html>
